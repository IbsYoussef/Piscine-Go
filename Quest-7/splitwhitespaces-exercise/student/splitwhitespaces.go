package student

// TODO: Write your code here
// Split string on whitespace characters
//
// Hints:
// - Function signature: func SplitWhiteSpaces(s string) []string
// - Initialize: result := []string{}, word := ""
// - Loop through characters: for _, char := range s
// - Check whitespace: if char == ' ' || char == '\t' || char == '\n'
// - If whitespace and word not empty: append word, reset word
// - If not whitespace: add char to word (word += string(char))
// - After loop: append last word if not empty
//
// Edge cases:
// - Multiple consecutive spaces: "a  b" → ["a", "b"]
// - Leading/trailing whitespace: "  trim  " → ["trim"]
//
// Examples:
//   SplitWhiteSpaces("Hello how are you?")
//   Returns: []string{"Hello", "how", "are", "you?"}

func SplitWhiteSpaces(s string) []string {
	// Your code here
	return nil
}
