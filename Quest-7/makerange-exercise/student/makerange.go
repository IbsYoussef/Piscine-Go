package student

// TODO: Write your code here
// Create a slice with values from min to max-1 using make
//
// Hints:
// - Function signature: func MakeRange(min, max int) []int
// - Check if min >= max: return nil
// - Calculate length: length := max - min
// - Create slice: result := make([]int, length)
// - Fill slice: for i := 0; i < length; i++ { result[i] = min + i }
// - Return result
//
// Key difference from AppendRange:
// - AppendRange uses append (dynamic)
// - MakeRange uses make (pre-allocated)
//
// Examples:
//   MakeRange(5, 10) returns [5, 6, 7, 8, 9]
//   MakeRange(10, 5) returns nil

func MakeRange(min, max int) []int {
	// Your code here
	return nil
}
