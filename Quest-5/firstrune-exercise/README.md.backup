# firstrune

**Status:** Required

## Problem Statement

Write a function that returns the first `rune` of a `string`.

## Expected Function Signature

```go
func FirstRune(s string) rune {

}
```

## Expected Output

```console
HSO
```

## Requirements

- Create a file named `firstrune.go`
- Define package as `package piscine`
- Implement the `FirstRune` function
- Return the first rune (character) of the string

## Submission Structure

```
firstrune.go
```

## How to Work on This

1. Write your solution in `student/firstrune.go`
2. Run `./run.sh` to see your output
3. Run `./test.sh` to check if it works
4. Compare with `solutions/firstrune.go` if you need help

## Hints

- Convert string to rune slice: `[]rune(s)`
- Access first element: `runes[0]`
- Alternatively, use range loop to get first rune
- Strings in Go are UTF-8 encoded
- Runes represent Unicode code points

## Example

```go
package main

import (
	"piscine"
	"github.com/01-edu/z01"
)

func main() {
	z01.PrintRune(piscine.FirstRune("Hello!"))
	z01.PrintRune(piscine.FirstRune("Salut!"))
	z01.PrintRune(piscine.FirstRune("Ola!"))
	z01.PrintRune('\n')
}
```

Output:

```
HSO
```

## Key Concepts

- **Runes**: Go's character type (int32)
- **String to rune conversion**: Handling Unicode
- **Indexing**: Accessing specific elements
- **UTF-8 encoding**: String representation in Go

## Notions

- [rune-literals](https://golang.org/ref/spec#Rune_literals)
